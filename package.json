{
  "name": "best-running-window",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "npx serve .",
    "build:css": "tailwindcss -i ./css/tailwind.input.css -o ./css/tailwind.css --minify"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "tailwindcss": "^3.4.19",
    "vitest": "^2.1.0"
  }
}
