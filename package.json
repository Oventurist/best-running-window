{
  "name": "best-running-window",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^2.1.0"
  }
}
